# Xcode
xcuserdata/
DerivedData/
*.xcuserstate
*.xccheckout
*.moved-aside
*.xcscmblueprint
# Xcode recreates this empty whenever the scheme is edited; it has never held
# a shared setting worth tracking
Deposplit.xcodeproj/project.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings

# macOS
.DS_Store

# Swift Package Manager (legacy — this repo previously contained an SPM package)
.build/
.swiftpm/
